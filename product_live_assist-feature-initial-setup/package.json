{
  "name": "sable-product-live-assist",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "packages/*"
  ],
  "scripts": {
    "build:sdk": "npm run build --workspace @sable/sdk-contracts && npm run build --workspace @sable/workflow-core && npm run build --workspace @sable/web-sdk && npm run build --workspace @sable/web-sdk-ui",
    "test:sdk": "npm run test --workspace @sable/sdk-contracts && npm run test --workspace @sable/workflow-core && npm run test --workspace @sable/web-sdk && npm run test --workspace @sable/web-sdk-ui && npm run test --workspace @sable/sdk-testkit",
    "test:sdk:browser": "npm run build:sdk && node --import tsx server/src/sdk-tests/gate1-browser.integration.test.ts",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace aidan-server && npm run test:sdk",
    "verify": "npm run typecheck && npm run test && npm run build:sdk"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
